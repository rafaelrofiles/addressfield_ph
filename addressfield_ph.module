<?php

/**
 * @file
 * Defines plugin for Philippines address.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function addressfield_ph_ctools_plugin_directory($module, $plugin) {
  if ($module == 'addressfield') {
    return 'plugins/' . $plugin;
  }
}
